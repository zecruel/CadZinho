name: Translate with AI
# Create translations GUI terms file using AI - Github models
on:
  workflow_dispatch:

permissions:
  models: read

jobs:
  translate:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Prepare Lua with cURL
        run: |
          sudo apt-get update
          sudo apt-get install --yes lua5.4 luarocks curl libcurl4 libcurl4-openssl-dev
          sudo luarocks install Lua-cURL CURL_INCDIR=/usr/include/x86_64-linux-gnu/curl/
      - uses: actions/checkout@v4.1.1
      - name: Run Lua script
        run: |
          cd translate
          lua translate_ai.lua
      - name: Generate zip file with results
        run: zip -r translate ./translate
      - name: Upload the result
        uses: actions/upload-artifact@v4.3.0
        with:
          name: translate
          path: translate.zip
          retention-days: 5
